# Ensure text files use LF in the repository and native line endings on checkout
* text=auto

# Explicitly set line endings for certain file types
*.py text eol=lf
*.sh text eol=lf
*.bat text eol=crlf

# Ignore line endings for binary files
*.jpg binary
*.png binary
*.exe binary
